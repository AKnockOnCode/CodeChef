#include <stdio.h>

int main() {
    int T;
    scanf("%d", & T);
    while (T--) {
        int x;
        scanf("%d", & x);
        if (x < 100) printf("Easy\n");
        else if (x < 200) printf("Medium\n");
        else printf("Hard\n");
    }
    return 0;
}