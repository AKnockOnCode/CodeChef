#include <stdio.h>

int main() {
    int T;
    scanf("%d", &T);
    while (T--) {
    int A, B, X;
    scanf("%d %d %d", &A, &B, &X);
    printf("%d\n", (B-A+(X-1))/X);
    }
    return 0;
}