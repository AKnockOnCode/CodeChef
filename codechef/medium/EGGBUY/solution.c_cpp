#include <stdio.h>

int main() {
    int X, Y, F;
    scanf("%d %d %d", &X, &Y, &F);
    int A = (X*12);
    int B = (Y*12) + F;
    int min = A<B?A:B;
    printf("%d\n", min);
    return 0;
}