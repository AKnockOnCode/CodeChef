#include <stdio.h>

int main() {
    int T;
    scanf("%d", &T);
    while (T--) {
    int N, K;
    scanf("%d %d", &N, &K);
    if (K>N){
        printf("%d\n", (K-N)*2);
    }
    else{
        printf("0\n");
    }
    }
    return 0;
}